# Auditd Rule Checker

A simple Bash script for checking and validating `auditd` rules.

## Examples
Check the default configuration:
```bash
$ ./auditd-rule-checker.sh
```

Check a custom rules file:
```
$ ./auditd-rule-checker.sh ./rules
```
